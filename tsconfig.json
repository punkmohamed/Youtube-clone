{
  "files": [],
  "references": [
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.node.json"
    }
  ],
  "compilerOptions": {
    "noEmitOnError": false, // Allows building even if there are TypeScript errors
    "skipLibCheck": true, // Skips type checking in declaration files for faster builds
    "strict": false // Loosens the strict type-checking options
  }
}